<?xml version="1.0" encoding="utf-8"?>
<feed xml:lang="{{ include.feed.lang }}" xmlns="http://www.w3.org/2005/Atom">
    <title type="text">{{ include.feed.title | xml_escape }}</title>
    <subtitle type="text">{{ include.feed.description | xml_escape }}</subtitle>
    <author>
        <name>Raal</name>
    </author>
    <updated>{% for entry in include.entries limit:1 %}{{ entry.date | date_to_xmlschema }}{% endfor %}</updated>
    <id>urn:uuid:{{ include.feed.uuid }}</id>
    <link rel="alternate" type="text/html" href="{{ site.url }}{{ include.feed.source }}" />
    <link rel="self" type="application/atom+xml" href="{{ site.url }}{{ include.feed.permalink }}" />
    <icon>{% unless include.feed.image contains "https://" %}{{ site.url }}{% endunless %}{{ include.feed.image }}</icon>
    {% for entry in include.entries %}
        {% include feeds/content/entry.xml feed=include.feed entry=entry %}
    {% endfor %}
</feed>