<item>
    <title>{{ include.item.title | xml_escape }}</title>
    <link>{{ site.url }}{{ include.channel.source }}#{{ include.item.cid }}</link>
    {% for file in include.item.files %}
        {% if include.channel.quality == "low" and file.type.name == "mp3" %}
            {% assign enclosure = file %}
        {% elsif include.channel.quality == "high" and file.type.name == "flac" %}
            {% assign enclosure = file %}
        {% endif %}
    {% endfor %}
    {% if enclosure %}
        <enclosure url="{% unless enclosure.path contains "https://" %}{{ site.url }}{% endunless %}{{ enclosure.path }}" length="{{ enclosure.size }}" type="{{ enclosure.type.mime }}" />
        <itunes:duration>{{ enclosure.length }}</itunes:duration>
    {% endif %}
    <itunes:image href="{% unless item.image.path contains "https://" %}{{ site.url }}{% endunless %}{{ item.image.path }}" />
    <guid>{{ site.url }}/itunes/{{ item.uuid }}</guid>
    <pubDate>{{ include.item.date | date_to_rfc822 }}</pubDate>
</item>